[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "strongtowns-detroit"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "strongtowns-graphics @ git+https://github.com/Strong-Towns-Detroit/strongtowns-graphics.git@8d11da7acd439a7e6186eaebd1a25c3458f82b6a",
    "strongtowns-data @ git+https://github.com/Strong-Towns-Detroit/strongtowns-data.git@5637d6a7b327d8b173a6d2f53560fd127f077372",
    "osmnx",
    "geopandas",
    "networkx",
    "shapely",
    "scikit-learn",
    "matplotlib",
    "pandas",
    "polars",
    "numpy",
    "pyarrow",
    "jsonschema",
    "requests",
    "python-dotenv",
    "pyogrio",
    "beautifulsoup4",
    "lxml",
    "python-docx",
    "rdflib",
    "pillow",
    "httpx",
    "duckdb>=1.4,<2",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-mock"]
minutes = ["google-genai", "pydantic"]
archive = ["boto3"]
census = ["pytidycensus"]
semantic = ["sentence-transformers"]
accessibility = ["r5py"]
notebooks = ["marimo>=0.18,<1"]

[tool.pytest.ini_options]
testpaths = [
    "tests",
    "projects/detroit-land-use-forum",
]
pythonpath = [
    ".",
    "src",
    "pipelines/legislative-district",
]
