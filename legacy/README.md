# Legacy workflows

These files are retained for provenance, not production use, and are excluded
from `strongtowns-data`. New work requires a registered, tested pipeline with
explicit inputs and immutable snapshots.
