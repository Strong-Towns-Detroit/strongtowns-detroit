[project]
name = "zoning-graphiti-assist"
version = "0.1.0"
description = "Non-authoritative Graphiti extraction memory for the zoning ontology workflow"
requires-python = ">=3.12"
dependencies = [
  "graphiti-core==0.29.3",
  "python-dotenv>=1.1,<2",
]

[tool.uv]
package = false
